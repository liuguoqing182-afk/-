# AM 首屏配置巡检系统阶段历史记录

> 记录日期：2026-07-23  
> 用途：为后续功能文档、方案评审和其他 AI 接续工作保存已经确认的事实。  
> 说明：本文件是阶段事实记录，不等同于最终产品功能文档。

## 1. 当前阶段结论

AM 首屏配置巡检系统分为两个阶段：

1. 第一阶段：读取正式群发布通知，解析发布声明，执行后台配置巡检，并把报告投递到测试群。
2. 第二阶段：根据发布变更选择手机端检查任务，控制实体 Android 手机打开 AIMirror，执行页面检查、真实渲染、截图取证和拼图报告。

当前状态：

- 第一阶段基本完成并进入测试群观察期。
- 第二阶段已完成技术可行性验证和一次 AI Better 真实渲染验证。
- 第二阶段尚未完成“发布消息 → 任务路由 → 手机自动化 → 拼图报告”的正式自动触发闭环。
- 当前暂停编写和批量运行 App 自动化脚本，先完成模块与功能文档。

## 2. 第一阶段已经完成的能力

### 2.1 正式发布消息读取

- 智能牛马每隔一小时读取正式群历史消息。
- 只识别以 `AIMirror首屏配置发布成功!` 开头的发布通知。
- 使用飞书 `message_id` 持久化去重，避免重复巡检。
- 分页读取历史消息，并使用时间重叠窗口避免小时边界漏消息。

### 2.2 配置巡检

- 解析发布声明中的配置模块和变更内容。
- 读取 DEV、PRO 和发布前基线。
- 对比实际配置变化、发布声明和资源 URL。
- 保存原始快照、结构化检查结果、环境备份和报告。
- 已知历史差异与本次新增问题分开处理。

### 2.3 报告隔离

- 正式群只作为正式发布通知的只读来源。
- 当前观察阶段，巡检结果只发送到 `FEISHU_TEST_CHAT_ID` 对应的测试群。
- 启动时强制检查测试群与正式群 ID 不相同。
- 正式群报告投递尚未启用。

### 2.4 Windows 常驻运行

- 计划任务名称：`AIMirror-AM-Config-History-Poller-Test`。
- 使用 Windows AtStartup 方式启动。
- 进程异常退出后按一分钟间隔重启。
- 同一轮询器只允许一个实例运行。

## 3. 发布配置模块统计

统计范围：正式群最近 400 条消息。

- 实际读取消息：400 条。
- AIMirror 发布通知：376 条。
- 发布通知可以同时包含多个模块，因此模块出现次数之和大于发布通知数量。

| 配置模块 | 包含该模块的发布通知数 | 已解析变更项数 | 主要内容 |
|---|---:|---:|---|
| 模版修改 | 376 | 3,141 | 新增/删除模型、模型图片、封面图系列、模型字段 |
| 新首页配置 | 326 | 3,895 | 标签排序、模型归属、模型排序、组封面 |
| More Style AI Filter 配置 | 268 | 1,667 | Filter 标签、模型增删和排序、组封面 |
| 国际化配置 | 251 | 1,761 | 国际化模型新增、删除及语言配置 |
| More Style Video 配置 | 185 | 906 | Video 标签、视频模型增删和排序、组封面 |
| 新手引导页配置 | 56 | 65 | 引导页新增、删除、修改和排序 |

补充说明：

- 六个配置模块合计出现 1,462 次。
- “模版修改”和“新首页配置”出现频率最高，是第二阶段优先设计对象。
- 模块标题统计可靠；部分旧通知格式无法完全解析，因此“已解析变更项数”不能代表全部历史实际变更量。

## 4. 当前 App 功能候选模块

现有 AIMirror Android 自动化工程中共有 12 个候选功能用例：

1. Removal
2. AI Background
3. Dress-Up
4. Enhancer
5. Makeup
6. AI Muscle
7. AI Salon
8. Magic Brush
9. AI Restoration
10. Chat Edit
11. AI Better
12. Live Photo

当前用例结构统计：

- 共 12 个功能模块。
- 共 38 个用例步骤。
- 每个模块包含 1 个真实渲染提交步骤。
- 正常完成一轮全量回归会触发 12 次真实渲染。
- 失败情况下存在渲染重试和模块重跑机制，因此正式接入前必须定义消费次数、重试上限和费用保护。

当前实际执行情况：

- AI Better：已真实执行 1 次并通过。
- 其他 11 个功能模块：本阶段暂未触发。

## 5. 第二阶段实体手机验证记录

### 5.1 设备连接

- 连接方式：USB ADB。
- 设备序列号：`R38M805JQHM`。
- 设备型号：Samsung Note10+。
- 设备槽位：`midscene-android-1`。
- AIMirror Android 包名：`com.ai.polyverse.mirror`。
- AIMirror 版本：7.1.0，versionCode 210。

已经验证：

- ADB 能识别设备。
- 设备守护进程能发现设备并标记为 `ready`。
- 可以把手机从 `Dozing` 唤醒为 `Awake`。
- 可以自动关闭旧进程并启动 AIMirror `MainActivity`。
- 可以从遗留侧栏或非首页状态恢复到出现 `Inspiration` 的首页。
- 可以通过 ADB 抓取界面 XML 和手机截图。

### 5.2 设备守护进程遗留问题

设备守护进程当前不阻塞 Android 使用，但存在两个待处理问题：

1. Windows 环境仍会周期性尝试执行 iOS `idevice_id` 扫描，产生无关错误日志。
2. 读取旧数据库时查询不存在的 `mcpServer` 列，产生兼容性错误日志。

上述问题会导致日志持续增长，正式长期运行前需要修复。

## 6. AI Better 真实渲染验证

### 6.1 执行结果

- 会话 ID：`20260723094252`。
- 执行模块：`ai-better`。
- 结果：PASS。
- 步骤：3/3 通过。
- 尝试次数：1 次。
- 总耗时：333 秒。
- 时间：约 17:42 至 17:48。

验证流程：

1. 进入 AI Better 并选择照片。
2. 点击 Create，处理云上传提示并进入渲染。
3. 等待渲染结束，验证 Redraw 和可用的 Save 按钮，保存最终截图。

### 6.2 静默执行规则

真实验证时显式设置：

```text
FEISHU_NOTIFY_ENABLED=0
FEISHU_CHAT_ID=
```

静默重跑没有向正式群或测试群发送启动、进度和完成文字消息。

在用户明确建立“全流程跑通前正式群零消息”规则之前，第一次尝试已经发送过一条“测试已启动”通知；该进程随后被停止，没有发送完成通知。此事件作为防误发案例保留，后续应从代码和配置层增加硬隔离，而不能只依赖运行命令临时关闭通知。

## 7. 拼图报告验证

本次从 Midscene HTML 报告中提取了三张关键过程帧：

1. 进入 AI Better 并选好照片，Create 按钮可见。
2. 点击 Create 后进入 `Analyzing Photo` 渲染状态。
3. 渲染成功，Redraw 和紫色 Save 按钮可见。

拼图样式：

- 深色背景。
- 左侧显示模块名、PASS/FAIL、步骤数、耗时、时间和设备。
- 右侧按时间顺序排列手机截图。
- 底部显示步骤说明。
- 通过步骤使用绿色。
- 失败步骤和失败结论使用红色。

本次拼图：

```text
D:\Users\lgq\自动化\eagleclaw\mcp-servers\eagleclaw-Aimirror\logs\reports\20260723094252\ai-better-result.jpg
```

图片尺寸：1328 × 828。

该图片已明确发送到测试群，飞书返回消息 ID：

```text
om_x100b692a235e00bcb26b41a4e395dfb
```

发送前已校验目标群等于用户环境变量 `FEISHU_TEST_CHAT_ID`，且不等于正式群 `FEISHU_CHAT_ID`。

## 8. 已确定的强制规则

1. 整个“发布消息 → 配置解析 → 任务路由 → 手机执行 → 截图报告”链路没有跑通并验收前，正式群不得发送任何测试消息。
2. 正式群只允许出现真实的正式发布通知和未来经过批准的正式巡检结果。
3. 开发、联调、失败报告、测试截图和状态通知只能发送到测试群。
4. 手机自动化不能把模型名、模型 ID、标签名和排序写死在脚本中。
5. 固定的是“变更类型对应什么检查”，实际模型、标签和语言参数来自发布消息与线上配置。
6. 一次发布只运行受影响的检查，不默认执行全部 12 个渲染模块。
7. 未执行、无法定位或无法验证的项目不能标记为通过。
8. 每次真实渲染必须记录消费次数、重试次数、截图和最终结果。

## 9. 模块关系的当前定义

第二阶段不应直接把六类配置模块和十二个 App 功能模块一一写死绑定。

正确关系：

```text
六类发布配置
  → 解析具体变化对象
  → 生成结构化检查计划
  → 选择通用页面检查或对应 App 功能流程
  → 实体手机执行
  → 截图与报告
```

示例：

- `模型Chat Edit有字段改动` → 选择 Chat Edit 功能检查。
- `模型AI Better图片改变` → 选择 AI Better 卡片、详情和渲染检查。
- `标签Painting的模型排序改变` → 检查 AI Filter 的 Painting 标签和模型顺序，不需要执行全部功能。
- `新增国际化模型984` → 根据模型 984 的线上配置和受影响语言执行显示检查。
- 未知新模型 → 使用参数化通用模型检查流程，不能因为没有预置 YAML 就直接跳过。

## 10. 功能文档下一步需要明确的内容

1. 六类配置模块分别包含哪些变化类型。
2. 每种变化类型在手机端需要验证哪些页面和字段。
3. 哪些检查只需要页面展示，哪些必须触发真实渲染。
4. 模型、标签、语言和入口如何从线上配置动态解析。
5. 一条发布消息如何拆成一个或多个手机任务。
6. 多个变更指向同一模型时如何合并任务。
7. 一次发布允许消耗多少次渲染额度。
8. 网络错误、服务异常、次数不足和付费弹窗的状态定义。
9. PASS、FAIL、BLOCKED、NOT_CHECKED 的判定标准。
10. 测试群报告内容、截图数量、拼图布局和保留期限。
11. 正式群启用条件及人工审批流程。
12. 手机离线、电脑重启、任务中断和报告发送失败的恢复策略。

## 11. 关键文件位置

第一阶段项目：

```text
D:\Users\lgq\自动化\am-config-inspector
```

第一阶段总体总结：

```text
D:\Users\lgq\自动化\am-config-inspector\AM首屏配置巡检系统总结与跨项目复用指南.md
```

Android 自动化运行器：

```text
D:\Users\lgq\自动化\eagleclaw\mcp-servers\eagleclaw-Aimirror
```

现有 App 功能 YAML：

```text
D:\Users\lgq\自动化\eagleclaw\test-plans\test-plans\am\yaml
```

AI Better 结果摘要：

```text
D:\Users\lgq\自动化\eagleclaw\mcp-servers\eagleclaw-Aimirror\logs\am-summary-20260723094252.json
```

AI Better 最终截图：

```text
D:\Users\lgq\自动化\eagleclaw\mcp-servers\eagleclaw-Aimirror\logs\screenshots\20260723094252_0\ai-better\step-3.png
```

AI Better 拼图报告：

```text
D:\Users\lgq\自动化\eagleclaw\mcp-servers\eagleclaw-Aimirror\logs\reports\20260723094252\ai-better-result.jpg
```

## 12. 当前停止点

截至本记录生成时：

- 不继续批量执行 App 自动化模块。
- 不编写新的自动化检测脚本。
- 不向正式群发送任何消息。
- 先基于本记录编写和评审第二阶段功能文档。

